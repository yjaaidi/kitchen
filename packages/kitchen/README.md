# kitchen

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build kitchen` to build the library.

## Running unit tests

Run `nx test kitchen` to execute the unit tests via [Vitest](https://vitest.dev/).
